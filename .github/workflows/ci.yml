name: tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install GCC 14
        run: sudo apt-get update && sudo apt-get install -y gcc-14

      - name: Build and test
        run: make clean && make CC=gcc-14 test
